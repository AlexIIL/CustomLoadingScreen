package alexiil.mods.load.baked.func;

/** Later this will allow for ASM generating classes for functions, which would (hopefully) be quicker than the
 * hard-coded versions */
public interface ICompilableFunction<T> {}
